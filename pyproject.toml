[project]
name = "api-photovault"
version = "0.1.0"
description = "API PhotoVault"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "minio>=7.2.20",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.22",
    "uvicorn>=0.41.0",
]
